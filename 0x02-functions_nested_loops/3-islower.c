#include "main.h"
/**
 * _islower - Check if character is in lower case
 * 
 * Return: true - 1, false - 0;
 */
int _islower(int c)
{
	int str;

	if (c < 97 || c > 122)
		str = _putchar(48);
	str = _putchar(49);
	return (str);
}
